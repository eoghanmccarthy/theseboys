import { css } from '@emotion/core';

const sample = css`
  display: flex;
  padding-right: 5px;
  margin-right: 5px;
  border-right: 1px dotted grey;
  > button {
    background-color: var(--color-secondary);
  }
`;

export { sample };
