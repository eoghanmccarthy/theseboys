import { css } from '@emotion/core';

const oscillator = css`
  transform: skewX(-8deg);
`;

const pad = css`
  width: 200px;
  height: 200px;
  background-color: blue;
`;

export { pad, oscillator };
